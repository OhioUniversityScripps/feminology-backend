if (Meteor.isServer) {
  // Houston.add_collection(Meteor.users);
	// Houston.add_collection(Posts);
	// Houston.add_collection(Comments);
	// Houston.add_collection();
	// Houston.add_collection(collection);
};
